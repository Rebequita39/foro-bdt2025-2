    {
       "watch": ["server.js"],
       "ext": "js,json",
       "ignore": ["node_modules"],
       "exec": "node server.js"
     }
     ```
   - Modifica el archivo `package.json` para usar Nodemon:
     ```json
     "scripts": {
       "start": "nodemon"
     }
